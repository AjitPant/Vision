<Container>:
    display: display
    rows: 1
    BoxLayout:
        orientation: "vertical"
        MDButton:
            on_release: root.MD()
        POCRButton:
            on_release: root.POCR()
        LLRButton:
            on_release: root.LLR()
        HCButton:
            on_release: root.HC()
