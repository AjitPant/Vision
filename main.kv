<Container>:

    rows: 2
    BoxLayout:
        orientation: "horizontal"
        MDButton:
            on_release: root.MD()
        POCRButton:
            on_release: root.POCR()
    BoxLayout:
        orientation: "horizontal"
        LLRButton:
            on_release: root.LLR()
        HCButton:
            on_release: root.HC()
